const int maxn = 1005;
int a[5], b[5];

int main() {
	a[0] = 1;
	a[1] = 2;
	a[2] /= 2;
	a[3] *= 5;
	a[4] -= 2;
	a[0] &= (1 << 3);
	a[2]++;
	++a[3];
	if (a[0] >= a[1]) a[0] += a[2]; // this is a comment
	/* this is a comment too */
	return 0;
}